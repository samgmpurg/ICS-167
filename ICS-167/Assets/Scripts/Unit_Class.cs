using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public abstract class UnitInstance : MonoBehaviour
{
    /*[SerializeField]
    private NavMeshAgent agent;
    public void Initalize(string name, SOUnitDefinition.UnitTypes type,Player owner){
        UnitName = name;
        Type = type;
        agent.SetDestination(); //This needs to be finished when we set the spawn position spots of each player
    }
    //Need to implement a - Remove units funtion, unit Attack function, unit move function(navmesh probably)
*/
}
